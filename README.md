# BasicSpecflowTests
These basic Specflow tests cover off a few key features:
1) Use of ScenarioContext class to switch data between steps
2) Use of Scenario Outline and Examples in order to parameterise tests
3) General ordering for a specflow framework
